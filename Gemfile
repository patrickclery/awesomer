# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

gem 'octokit'      # GitHub API client
# gem 'httparty'   # (optional) for general HTTP requests
gem 'sequel'       # Database toolkit
gem 'sqlite3'      # SQLite DB for local development
gem 'dotenv'       # Load environment variables from .env
